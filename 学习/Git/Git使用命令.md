# GIT username userEmail 注册

git config --global user.name "Your Name"  
git config --global user.email "email@example.com"

# GIT 仓库初始化 git init

创建目录 mkdir gitTest/
对该目录进行仓库初始化 **git init** 会生成.git 文件夹,表示这个目录变成了 Git 可以管理的仓库(初始化的仓库都是空的)
